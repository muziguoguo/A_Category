use_frameworks!

platform :ios, '8.0'

target 'A_Category_Example' do
  pod 'A_Category', :path => '../'

  target 'A_Category_Tests' do
    inherit! :search_paths

    pod 'FBSnapshotTestCase'
  end
end
